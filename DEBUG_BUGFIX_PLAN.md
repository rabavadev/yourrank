# YourRank Debug Bug-Fix Plan

Generated by Aiden on 2026-07-04T04:43:31.774335+00:00.

## How to run in debug mode from the repo

### Leaderboard app

Repo-provided dev command:

```bash
cd apps/leaderboard
bun run dev
```

This runs Wrangler locally using the script from `apps/leaderboard/package.json`:

```json
"dev": "wrangler dev"
```

### Bot app

Repo-provided dev commands:

```bash
cd apps/bot
bun run dev
```

and Worker-mode dev:

```bash
cd apps/bot
bun run dev:worker
```

These come from `apps/bot/package.json`:

```json
"dev": "tsx watch src/index.ts",
"dev:worker": "wrangler dev"
```

### Notes

- The leaderboard debug path is clearly Worker-oriented via Wrangler.
- The bot has two local debug paths documented by scripts, but the deployed entrypoint is `src/worker.ts` in `apps/bot/wrangler.toml`.
- For webhook testing during local debug, the repo README already notes a public tunnel is needed.

## Observable issues found during repository inspection

### BUG-001: README repo identity drift
**Severity:** Medium

`README.md` still refers to the old product/repository name `GroupsMix` and uses the old clone path `rabavadev/groupsmix.git`, while the actual repository inspected is `rabavadev/yourrank`.

**Impact:**
- Misleads contributors during setup.
- Breaks copy-paste clone/bootstrap steps.
- Makes local debugging harder because the documented repo path does not match reality.

**Fix implemented:** Yes

---

### BUG-002: Secrets inventory drift for Hyperdrive binding
**Severity:** Medium

`SECRETS.md` documents the shared Hyperdrive binding id as:

```text
f24c020e169848448e4ab9ca7d24ac23
```

But both live Worker configs inspected in the repo use:

```text
9e5b625bfdd84cd691b44c4780bdaf13
```

**Impact:**
- Operators may configure or audit against the wrong infrastructure id.
- Makes environment debugging and deploy validation harder.
- Increases risk of config mistakes when reproducing issues.

**Fix implemented:** Yes

---

### BUG-003: Secrets inventory/product naming drift
**Severity:** Low

`SECRETS.md` still refers to `GroupsMix` / `groupsmix-*` in multiple places even though the current repo/app identity is `yourrank` / `yourrank-*` / `yourrank.site`.

**Impact:**
- Operational confusion during debugging and setup.
- Inconsistent naming across docs and deployed config.

**Fix implemented:** Yes

---

### BUG-004: Workflow inspection tool mismatch / incomplete direct CI visibility
**Severity:** Low

While inspecting CI, `list_workflow_runs` returned:

```text
['missing required parameter: workflow_id']
```

This means direct workflow-run retrieval through the available tool requires an additional workflow identifier. The repository itself still exposes enough evidence via workflow files and prior merged PRs to identify concrete issues.

**Impact:**
- Prevented direct run-history confirmation in this session.
- Did not block repository-level fixes.

**Fix implemented in code:** No repo code change required.

## Changes made

1. Corrected `README.md` to reflect the actual repository/product identity.
2. Corrected `SECRETS.md` naming drift from `GroupsMix/groupsmix` to `YourRank/yourrank` where operationally relevant.
3. Corrected `SECRETS.md` Hyperdrive binding id to match the actual Worker configs in the repository.
4. Added this debug/bug-fix plan document.

## Why these fixes first

Because this session is scoped to GitHub repository inspection only, I can safely fix the bugs that are directly observable from repo state without inventing runtime behavior or requiring hidden infrastructure access.

The repo already contains many recently merged functional fixes; the clearest remaining observable problems in the current default branch are documentation/config drifts that directly affect debugging, setup, and safe ops.

## Follow-up checks recommended

1. Verify GitHub Actions repository secrets exist for:
   - `CLOUDFLARE_API_TOKEN`
   - `CLOUDFLARE_ACCOUNT_ID`
   - `SUPABASE_ACCESS_TOKEN`
   - `SUPABASE_DB_PASSWORD`
2. Confirm the staging Hyperdrive ids are intentionally unset before attempting staging deploys.
3. If you want deeper runtime debugging next, provide one of:
   - a failing issue/URL/PR,
   - a workflow id for CI log inspection,
   - or permission/context to target a specific failure path.
