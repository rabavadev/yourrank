// Auto-generated. Do not edit. Asset files inlined as strings.
export const leaderboard_css = "/* ============================================================\n   ChuckyBTZ x Stake — styled after theprotagonist.me\n   Purple→black field · cyan-blue name gradient · stream window\n   · 3-col partner panel · brand-colored social cards\n   ============================================================ */\n:root {\n  --bg:        #06040c;\n  --violet-1:  #1c0b38;\n  --violet-2:  #0d0720;\n  --panel:     #120e22;\n  --panel-2:   #171331;\n  --line:      rgba(150, 120, 220, 0.16);\n  --line-2:    rgba(150, 120, 220, 0.28);\n\n  --ink:       #f3f0ff;\n  --ink-soft:  #b7b2d4;\n  --ink-mute:  #7d789c;\n\n  /* the signature name gradient */\n  --cy:        #46d4ff;\n  --bl:        #6a86ff;\n  --grad-name: linear-gradient(100deg, #5ad9ff 0%, #7b8cff 60%, #9d7bff 100%);\n  --grad-cta:  linear-gradient(100deg, #6a86ff, #9d5bff);\n\n  /* brand colors */\n  --discord: #5865f2;\n  --kick:    #53fc18;\n  --twitch:  #9146ff;\n  --x:       #1d1d1f;\n  --gold:    #ffcb45;\n\n  --radius: 20px;\n  --radius-sm: 13px;\n  --wrap: 1140px;\n  --ease: cubic-bezier(0.16, 1, 0.3, 1);\n}\n\n* { box-sizing: border-box; margin: 0; padding: 0; }\nhtml { scroll-behavior: smooth; }\n\nbody {\n  font-family: \"Sora\", system-ui, sans-serif;\n  background: var(--bg);\n  color: var(--ink);\n  line-height: 1.55;\n  -webkit-font-smoothing: antialiased;\n  overflow-x: hidden;\n  position: relative;\n}\n::selection { background: var(--bl); color: #fff; }\n\n/* ---------- Ambient field ---------- */\n.field {\n  position: fixed; inset: 0; z-index: -3;\n  background:\n    radial-gradient(1000px 600px at 50% -10%, var(--violet-1) 0%, transparent 60%),\n    radial-gradient(800px 500px at 85% 10%, rgba(120,60,220,0.28) 0%, transparent 55%),\n    radial-gradient(700px 500px at 10% 20%, rgba(70,120,255,0.16) 0%, transparent 55%),\n    linear-gradient(180deg, var(--violet-2) 0%, var(--bg) 55%);\n}\n.watermarks { position: fixed; inset: 0; z-index: -2; overflow: hidden; opacity: 0.05; }\n.watermarks span {\n  position: absolute; font-family: \"Sora\"; font-weight: 800; white-space: nowrap;\n  color: #fff; text-transform: uppercase; letter-spacing: 0.05em;\n}\n\nsection { width: min(var(--wrap), 100% - 2.5rem); margin-inline: auto; }\n\n/* ---------- Nav ---------- */\n.nav {\n  width: min(var(--wrap), 100% - 2.5rem); margin-inline: auto;\n  display: flex; align-items: center; justify-content: space-between; padding: 1.4rem 0;\n}\n.nav-brand { font-weight: 800; font-size: 1.2rem; text-decoration: none; color: var(--ink); letter-spacing: -0.01em; }\n.nav-links { display: flex; gap: 1.6rem; }\n.nav-links a { color: var(--ink-soft); text-decoration: none; font-size: 0.9rem; font-weight: 500; transition: color 0.2s; }\n.nav-links a:hover { color: var(--ink); }\n\n/* ---------- Buttons ---------- */\n.btn {\n  display: inline-flex; align-items: center; justify-content: center; gap: 0.4rem;\n  font-family: \"Sora\"; font-weight: 700; font-size: 0.95rem; text-decoration: none;\n  padding: 0.8rem 1.5rem; border-radius: 12px; border: none; cursor: pointer;\n  color: #fff; transition: transform 0.18s var(--ease), box-shadow 0.2s var(--ease), filter 0.2s;\n}\n.btn--grad { background: var(--grad-cta); box-shadow: 0 10px 30px -10px rgba(120,80,255,0.7); }\n.btn--grad:hover { transform: translateY(-2px); box-shadow: 0 16px 38px -10px rgba(120,80,255,0.8); filter: saturate(1.1); }\n.btn--ghost { background: rgba(255,255,255,0.05); border: 1px solid var(--line-2); color: var(--ink); }\n.btn--ghost:hover { background: rgba(255,255,255,0.1); }\n.btn--full { width: 100%; }\n.btn--code { background: rgba(255,255,255,0.06); border: 1px solid var(--line-2); font-family: \"JetBrains Mono\", monospace; letter-spacing: 0.02em; }\n.btn--code:hover { background: rgba(255,255,255,0.12); }\n.btn--code.copied { background: var(--kick); color: #04240a; border-color: var(--kick); }\n\n/* ---------- Hero ---------- */\n.hero { text-align: center; padding: 4.5rem 0 3.5rem; position: relative; }\n.hero-kicker { font-size: 1rem; color: var(--ink-soft); letter-spacing: 0.02em; margin-bottom: 0.4rem; }\n.hero-name {\n  font-weight: 800; font-size: clamp(3rem, 11vw, 7.5rem); line-height: 0.95; letter-spacing: -0.02em;\n  background: var(--grad-name); -webkit-background-clip: text; background-clip: text; color: transparent;\n  padding-bottom: 0.1em;\n}\n.hero-sub { color: var(--ink-soft); font-size: 1.15rem; margin: 1rem auto 2rem; max-width: 44ch; }\n.hero-cta { display: flex; gap: 0.9rem; justify-content: center; flex-wrap: wrap; }\n\n/* ---------- Hero countdown ---------- */\n.hero-timer { margin-top: 2.4rem; }\n.timer-label {\n  font-size: 0.72rem; letter-spacing: 0.16em; text-transform: uppercase;\n  color: var(--ink-mute); margin-bottom: 0.85rem;\n}\n.timer-label [data-pool] { color: var(--cy); font-weight: 700; }\n.timer-grid {\n  display: inline-flex; align-items: flex-start; gap: 0.55rem;\n  padding: 0.9rem 1.15rem; border-radius: var(--radius-sm);\n  background: var(--panel); border: 1px solid var(--line);\n}\n.tcell { display: flex; flex-direction: column; align-items: center; min-width: 3.4rem; }\n.tcell b {\n  font-family: \"JetBrains Mono\", monospace; font-weight: 700;\n  font-size: 2rem; line-height: 1; color: var(--ink);\n  font-variant-numeric: tabular-nums; letter-spacing: -0.01em;\n}\n.tcell span {\n  margin-top: 0.5rem; font-size: 0.6rem; letter-spacing: 0.14em;\n  text-transform: uppercase; color: var(--ink-mute);\n}\n.tsep {\n  font-family: \"JetBrains Mono\", monospace; font-weight: 700; font-size: 1.6rem;\n  line-height: 1; color: var(--line-2); padding-top: 0.15rem;\n}\n/* seconds cell gets the accent so the live tick reads as \"alive\" */\n.tcell:last-of-type b { color: var(--cy); }\n@media (max-width: 480px) {\n  .timer-grid { gap: 0.3rem; padding: 0.75rem 0.65rem; }\n  .tcell { min-width: 2.7rem; }\n  .tcell b { font-size: 1.55rem; }\n  .tsep { font-size: 1.2rem; }\n}\n\n/* floating stream window */\n.stream-window {\n  position: absolute; left: 0; top: 3rem; width: 230px;\n  background: var(--panel); border: 1px solid var(--line-2); border-radius: 14px; overflow: hidden;\n  box-shadow: 0 30px 60px -25px rgba(0,0,0,0.8);\n  animation: float 6s ease-in-out infinite;\n}\n@keyframes float { 0%,100% { transform: translateY(0) rotate(-1.5deg); } 50% { transform: translateY(-14px) rotate(-1.5deg); } }\n.sw-bar { display: flex; align-items: center; gap: 0.6rem; padding: 0.55rem 0.8rem; background: rgba(255,255,255,0.03); border-bottom: 1px solid var(--line); }\n.sw-dots { display: flex; gap: 5px; }\n.sw-dots i { width: 9px; height: 9px; border-radius: 50%; background: #4a4463; }\n.sw-dots i:first-child { background: #ff5f57; } .sw-dots i:nth-child(2) { background: #febc2e; } .sw-dots i:nth-child(3) { background: #28c840; }\n.sw-title { font-size: 0.78rem; color: var(--ink-mute); font-weight: 600; }\n.sw-body { padding: 1.6rem 1rem; text-align: center; background: radial-gradient(circle at 50% 40%, rgba(83,252,24,0.12), transparent 70%); position: relative; }\n.sw-live { position: absolute; top: 0.6rem; left: 0.6rem; display: inline-flex; align-items: center; gap: 5px; font-size: 0.62rem; font-weight: 700; letter-spacing: 0.1em; color: var(--kick); }\n.live-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--kick); box-shadow: 0 0 8px var(--kick); animation: pulse 1.6s infinite; }\n@keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.35; } }\n.sw-play { width: 58px; height: 58px; margin: 0.5rem auto 0.8rem; border-radius: 50%; display: grid; place-items: center; color: #fff; background: rgba(255,255,255,0.08); border: 1px solid var(--line-2); }\n.sw-name { font-weight: 700; font-size: 0.95rem; }\n\n/* ---------- Partner panel ---------- */\n.panel {\n  position: relative; background: var(--panel); border: 1px solid var(--line); border-radius: var(--radius);\n  padding: 2.5rem 2rem 2rem; margin-top: 1rem;\n}\n.panel-badge {\n  position: absolute; top: -14px; left: 50%; transform: translateX(-50%);\n  font-size: 0.68rem; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase; color: var(--ink);\n  background: var(--grad-cta); padding: 0.35rem 1rem; border-radius: 999px;\n}\n.panel-grid { display: grid; grid-template-columns: 1.2fr 1fr 1.2fr; gap: 0; }\n.pcol { padding: 0.5rem 1.8rem; }\n.pcol + .pcol { border-left: 1px solid var(--line); }\n.pcol-label { display: block; font-size: 0.68rem; letter-spacing: 0.16em; text-transform: uppercase; color: var(--ink-mute); margin-bottom: 1rem; font-weight: 600; }\n.pcol-blurb { color: var(--ink-soft); font-size: 0.98rem; margin-bottom: 1.2rem; }\n.chips { list-style: none; display: flex; flex-wrap: wrap; gap: 0.5rem; }\n.chips li { font-size: 0.7rem; font-weight: 600; letter-spacing: 0.08em; text-transform: uppercase; color: var(--ink-soft); border: 1px solid var(--line-2); padding: 0.35rem 0.7rem; border-radius: 999px; }\n\n.code-box { background: rgba(90,60,180,0.15); border: 1px dashed var(--line-2); border-radius: 12px; padding: 0.9rem; text-align: center; margin-bottom: 0.7rem; }\n.code-val { font-family: \"JetBrains Mono\", monospace; font-weight: 700; font-size: 1.6rem; letter-spacing: 0.06em; color: #7fe0ff; text-shadow: 0 0 18px rgba(90,150,255,0.5); }\n.pcol-code .btn { margin-bottom: 0.6rem; }\n\n.why-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0.9rem; }\n.why { background: rgba(255,255,255,0.03); border: 1px solid var(--line); border-radius: var(--radius-sm); padding: 0.9rem; }\n.why-big { font-weight: 800; font-size: 1.5rem; line-height: 1; background: var(--grad-name); -webkit-background-clip: text; background-clip: text; color: transparent; }\n.why-label { display: block; font-size: 0.68rem; letter-spacing: 0.12em; text-transform: uppercase; color: var(--ink-soft); margin-top: 0.3rem; font-weight: 600; }\n.why-sub { display: block; font-size: 0.72rem; color: var(--ink-mute); margin-top: 0.3rem; }\n\n/* ---------- Section titles ---------- */\n.sec-title { font-weight: 800; font-size: clamp(1.9rem, 5vw, 2.8rem); letter-spacing: -0.02em; }\n.sec-title.center { text-align: center; }\n.sec-sub.center { text-align: center; color: var(--ink-soft); margin-top: 0.4rem; }\n.eyebrow { font-family: \"JetBrains Mono\", monospace; font-size: 0.82rem; letter-spacing: 0.04em; color: var(--cy); text-transform: uppercase; }\n\n/* ---------- Leaderboard ---------- */\n.board { padding: 4rem 0 3rem; }\n.board-head { display: flex; align-items: flex-end; justify-content: space-between; flex-wrap: wrap; gap: 1rem; margin-bottom: 2rem; }\n.board-meta { display: flex; gap: 1.5rem; }\n.bm { display: flex; flex-direction: column; align-items: flex-end; }\n.bm b { font-weight: 800; font-size: 1.3rem; }\n.bm span { font-size: 0.7rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--ink-mute); }\n.countdown { font-family: \"JetBrains Mono\", monospace; }\n\n.top3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.9rem; margin-bottom: 1.2rem; }\n.t3 {\n  position: relative; background: var(--panel); border: 1px solid var(--line); border-radius: var(--radius-sm);\n  padding: 1.3rem 1.1rem; text-align: center; overflow: hidden;\n  opacity: 0; transform: translateY(18px); animation: rise 0.6s var(--ease) forwards;\n}\n@keyframes rise { to { opacity: 1; transform: translateY(0); } }\n.t3--1 { border-color: rgba(255,203,69,0.5); box-shadow: 0 0 40px -16px var(--gold); animation-delay: 0.1s; }\n.t3--2 { animation-delay: 0s; } .t3--3 { animation-delay: 0.2s; }\n.t3-medal { font-family: \"JetBrains Mono\"; font-weight: 700; font-size: 0.75rem; letter-spacing: 0.1em; color: var(--ink-mute); }\n.t3--1 .t3-medal { color: var(--gold); }\n.t3-name { font-weight: 700; font-size: 1.05rem; margin: 0.5rem 0 0.3rem; word-break: break-all; }\n.t3-wager { font-family: \"JetBrains Mono\"; font-size: 0.82rem; color: var(--ink-soft); }\n.t3-prize { display: inline-block; margin-top: 0.7rem; font-weight: 800; font-size: 1.2rem; padding: 0.15rem 0.8rem; border-radius: 8px; background: rgba(255,255,255,0.05); }\n.t3--1 .t3-prize { color: var(--gold); background: rgba(255,203,69,0.12); }\n.t3--2 .t3-prize, .t3--3 .t3-prize { color: var(--cy); }\n\n.table { background: var(--panel); border: 1px solid var(--line); border-radius: var(--radius); overflow: hidden; }\n.t-head, .t-row { display: grid; grid-template-columns: 56px 1fr 150px 100px; align-items: center; gap: 0.5rem; padding: 0.8rem 1.4rem; }\n.t-head { background: rgba(255,255,255,0.03); border-bottom: 1px solid var(--line); font-size: 0.7rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--ink-mute); font-weight: 600; }\n.ta-r { text-align: right; }\n.t-rows { list-style: none; }\n.t-row { border-bottom: 1px solid var(--line); transition: background 0.15s; opacity: 0; animation: fade 0.4s ease forwards; }\n.t-row:last-child { border-bottom: none; }\n.t-row:hover { background: rgba(255,255,255,0.03); }\n@keyframes fade { to { opacity: 1; } }\n.tr-rank { font-weight: 800; color: var(--ink-mute); }\n.tr-player { display: flex; align-items: center; gap: 0.6rem; min-width: 0; }\n.tr-av { width: 28px; height: 28px; border-radius: 50%; flex-shrink: 0; display: grid; place-items: center; font-size: 0.72rem; font-weight: 700; background: rgba(120,90,255,0.2); border: 1px solid var(--line-2); }\n.tr-name { font-weight: 600; font-size: 0.92rem; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n.tr-wager { text-align: right; font-family: \"JetBrains Mono\"; font-size: 0.85rem; color: var(--ink-soft); }\n.tr-prize { text-align: right; font-weight: 700; }\n.tr-prize.has { color: var(--cy); } .tr-prize.no { color: var(--ink-mute); font-weight: 500; }\n\n.rules { margin-top: 1.2rem; background: var(--panel); border: 1px solid var(--line); border-radius: var(--radius-sm); padding: 0 1.3rem; }\n.rules summary { cursor: pointer; padding: 1rem 0; font-weight: 600; color: var(--ink-soft); list-style: none; }\n.rules summary::-webkit-details-marker { display: none; }\n.rules summary::before { content: \"＋ \"; color: var(--cy); }\n.rules[open] summary::before { content: \"－ \"; }\n.rules-list { list-style: none; padding: 0 0 1.2rem; display: grid; gap: 0.6rem; }\n.rules-list li { display: flex; gap: 0.7rem; color: var(--ink-soft); font-size: 0.9rem; }\n.rules-list li::before { content: \"\"; flex-shrink: 0; margin-top: 0.5rem; width: 6px; height: 6px; border-radius: 2px; background: var(--grad-cta); }\n\n/* ---------- Social cards ---------- */\n.socials-sec { padding: 3rem 0 4rem; }\n.social-cards { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; margin-top: 2rem; }\n.scard {\n  background: var(--panel); border: 1px solid var(--line); border-radius: var(--radius);\n  padding: 1.6rem 1.3rem; text-align: center; transition: transform 0.25s var(--ease), border-color 0.25s;\n}\n.scard:hover { transform: translateY(-5px); border-color: var(--line-2); }\n.scard-ico { width: 54px; height: 54px; margin: 0 auto 1rem; border-radius: 15px; display: grid; place-items: center; color: #fff; }\n.scard-ico svg { width: 26px; height: 26px; }\n.scard-ico.discord { background: var(--discord); } .scard-ico.kick { background: var(--kick); color: #04240a; }\n.scard-ico.twitch { background: var(--twitch); } .scard-ico.x { background: var(--x); border: 1px solid var(--line-2); }\n.scard-name { font-weight: 700; font-size: 1.1rem; }\n.scard-handle { font-size: 0.82rem; color: var(--ink-mute); margin: 0.15rem 0 1rem; }\n.scard .btn { width: 100%; }\n\n/* ---------- Footer ---------- */\n.ftr { width: min(var(--wrap), 100% - 2.5rem); margin: 0 auto; padding: 2.5rem 0; border-top: 1px solid var(--line); text-align: center; }\n.ftr-id { margin-bottom: 1rem; }\n.ftr-name { display: block; font-weight: 800; font-size: 1.2rem; }\n.ftr-tag { font-size: 0.85rem; color: var(--ink-mute); }\n.ftr-fine { color: var(--ink-mute); font-size: 0.78rem; max-width: 60ch; margin: 0 auto 0.7rem; line-height: 1.6; }\n.ftr-copy { color: var(--ink-mute); font-size: 0.8rem; }\n\n/* ---------- Responsive ---------- */\n@media (max-width: 940px) {\n  .stream-window { display: none; }\n  .panel-grid { grid-template-columns: 1fr; }\n  .pcol + .pcol { border-left: none; border-top: 1px solid var(--line); margin-top: 1.2rem; padding-top: 1.6rem; }\n  .social-cards { grid-template-columns: 1fr 1fr; }\n}\n@media (max-width: 600px) {\n  .nav-links { display: none; }\n  .top3 { grid-template-columns: 1fr; }\n  .t-head, .t-row { grid-template-columns: 40px 1fr 82px; }\n  .tr-wager, .t-head span:nth-child(3) { display: none; }\n  .social-cards { grid-template-columns: 1fr; }\n  .board-head { flex-direction: column; align-items: flex-start; }\n}\n@media (prefers-reduced-motion: reduce) {\n  *, *::before, *::after { animation: none !important; transition: none !important; }\n  .t3, .t-row { opacity: 1; transform: none; }\n  .stream-window { animation: none; transform: rotate(-1.5deg); }\n}\n\n/* --- RankUp badge (free plan) --- */\n.rk-badge{position:fixed;right:14px;bottom:14px;z-index:60;display:inline-flex;align-items:center;gap:6px;font-size:12px;font-weight:600;letter-spacing:.02em;color:#cfd3dd;background:rgba(10,10,16,.88);border:1px solid rgba(255,255,255,.14);border-radius:99px;padding:7px 13px;text-decoration:none;backdrop-filter:blur(6px)}\n.rk-badge b{color:#fff}\n.rk-badge:hover{border-color:rgba(255,255,255,.35);color:#fff}\n@media print{.rk-badge{display:none}}\n\n/* ---------- Logo (Pro branding) ---------- */\n.nav-brand { display: inline-flex; align-items: center; gap: 10px; }\n.nav-logo { height: 28px; width: auto; max-width: 120px; border-radius: 7px; display: block; }\n.hero-logo { height: 72px; width: auto; max-width: 200px; border-radius: 16px; margin: 0 auto 16px; display: block; box-shadow: 0 8px 32px rgba(0,0,0,0.45); }\n\n/* ---------- Prize breakdown strip ---------- */\n.payouts { display: flex; flex-wrap: wrap; align-items: center; gap: 8px; margin: 0 0 18px; }\n.pay-label { font-family: \"JetBrains Mono\", monospace; font-size: 11px; letter-spacing: .14em; text-transform: uppercase; color: var(--ink-mute); margin-right: 4px; }\n.pay { display: inline-flex; align-items: center; gap: 7px; font-family: \"JetBrains Mono\", monospace; font-size: 13px; color: var(--ink); background: var(--panel); border: 1px solid var(--line); border-radius: 99px; padding: 6px 13px; }\n.pay b { color: var(--gold); font-weight: 700; }\n\n/* ---------- Past winners ---------- */\n.past-sec { max-width: var(--wrap); margin: 70px auto 0; padding: 0 22px; }\n.past-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 14px; margin-top: 26px; }\n.past-card { background: var(--panel); border: 1px solid var(--line); border-radius: var(--radius-sm); padding: 16px 18px; }\n.past-label { font-family: \"JetBrains Mono\", monospace; font-size: 12px; letter-spacing: .1em; text-transform: uppercase; color: var(--ink-soft); border-bottom: 1px solid var(--line); padding-bottom: 10px; margin-bottom: 10px; }\n.past-list { list-style: none; margin: 0; padding: 0; }\n.past-row { display: flex; align-items: center; gap: 10px; padding: 6px 0; font-size: 14px; }\n.past-rank { width: 22px; height: 22px; border-radius: 6px; display: inline-flex; align-items: center; justify-content: center; font-family: \"JetBrains Mono\", monospace; font-size: 11px; font-weight: 700; background: var(--panel-2); color: var(--ink-mute); border: 1px solid var(--line); flex: none; }\n.past-rank.gold { color: #1a1405; background: linear-gradient(160deg, #ffe289, #d4a017); border-color: transparent; }\n.past-rank.silver { color: #14141c; background: linear-gradient(160deg, #e8e8f2, #9c9cb0); border-color: transparent; }\n.past-rank.bronze { color: #1d0f05; background: linear-gradient(160deg, #e8a86c, #a05c24); border-color: transparent; }\n.past-name { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n.past-val { font-family: \"JetBrains Mono\", monospace; font-size: 13px; color: var(--gold); }\n";
export const auth_js = "/* Shared logic for login / signup / forgot / reset pages.\n * Mode is derived from the URL path so no inline <script> is needed (the\n * auth pages run under a strict CSP with script-src 'self', which blocks\n * the previous inline window.__MODE__ assignment). */\nconst mode = { \"/signup\": \"signup\", \"/forgot\": \"forgot\", \"/reset\": \"reset\" }[location.pathname] || \"login\";\nconst form = document.getElementById(\"form\");\nconst errEl = document.getElementById(\"err\");\nconst msgEl = document.getElementById(\"msg\");\nconst submit = document.getElementById(\"submit\");\nconst nameInput = document.getElementById(\"name\");\nconst slugPreview = document.getElementById(\"slugPreview\");\nfunction slugify(s){return String(s||\"\").toLowerCase().trim().replace(/[^a-z0-9]+/g,\"-\").replace(/^-+|-+$/g,\"\").slice(0,40);}\nif (nameInput && slugPreview) nameInput.addEventListener(\"input\", () => { const s = slugify(nameInput.value); slugPreview.textContent = s ? \"yourrank.site/\" + s : \"yourrank.site/…\"; });\nif (mode === \"login\" || mode === \"signup\") {\n  fetch(\"/api/auth/me\").then(r => r.json()).then(d => { if (d && d.ok && d.user) location.href = \"/dashboard\"; }).catch(() => {});\n}\nform.addEventListener(\"submit\", async (e) => {\n  e.preventDefault(); errEl.textContent = \"\"; submit.disabled = true;\n  const orig = submit.textContent;\n  submit.textContent = { signup: \"Creating…\", login: \"Signing in…\", forgot: \"Sending…\", reset: \"Saving…\" }[mode] || \"Working…\";\n  let endpoint = \"/api/auth/\" + mode;\n  let payload;\n  if (mode === \"forgot\") {\n    payload = { email: document.getElementById(\"email\").value.trim() };\n  } else if (mode === \"reset\") {\n    const token = new URLSearchParams(location.search).get(\"token\") || \"\";\n    payload = { token, password: document.getElementById(\"password\").value };\n  } else {\n    payload = { email: document.getElementById(\"email\").value.trim(), password: document.getElementById(\"password\").value };\n    if (mode === \"signup\") payload.name = nameInput.value.trim();\n  }\n  try {\n    const res = await fetch(endpoint, { method: \"POST\", headers: { \"content-type\": \"application/json\" }, body: JSON.stringify(payload) });\n    const data = await res.json();\n    if (!res.ok || !data.ok) { errEl.textContent = data.error || \"Something went wrong.\"; submit.disabled = false; submit.textContent = orig; return; }\n    if (mode === \"forgot\") {\n      if (msgEl) { msgEl.hidden = false; msgEl.textContent = \"Done. If that account exists, a reset link is on its way. Check spam too.\"; }\n      form.querySelector(\"input\").disabled = true;\n      submit.textContent = \"Sent\";\n      return;\n    }\n    location.href = \"/dashboard\";\n  } catch (_) { errEl.textContent = \"Network error. Try again.\"; submit.disabled = false; submit.textContent = orig; }\n});\n";
export const leaderboard_js = "/* RankUp public leaderboard — hydrates from window.__SITE_DATA__ (SSR-injected). */\nconst $ = (s, r = document) => r.querySelector(s);\nconst $$ = (s, r = document) => [...r.querySelectorAll(s)];\nconst money = (n) => \"$\" + Number(n).toLocaleString(\"en-US\", { minimumFractionDigits: 2, maximumFractionDigits: 2 });\nconst moneyShort = (n) => \"$\" + Number(n).toLocaleString(\"en-US\", { maximumFractionDigits: 0 });\nconst initials = (name) => { const c = String(name).replace(/\\*/g, \"\").trim(); return c.length >= 2 ? c.slice(0, 2).toUpperCase() : (c ? c.toUpperCase() : \"★\"); };\nconst esc = (s) => String(s ?? \"\").replace(/[&<>\"']/g, (c) => ({ \"&\": \"&amp;\", \"<\": \"&lt;\", \">\": \"&gt;\", '\"': \"&quot;\", \"'\": \"&#39;\" }[c]));\n// Safe href: only allow http(s)/mailto/tel. Anything else (javascript:, data:,\n// vbscript:, etc.) collapses to \"#\". esc() handles attribute-breakout chars.\nconst safeUrl = (u) => { const s = String(u ?? \"\").trim(); return s && /^(https?:|mailto:|tel:)/i.test(s) ? esc(s) : \"#\"; };\nconst ord = (n) => { const s = [\"th\", \"st\", \"nd\", \"rd\"], v = n % 100; return n + (s[(v - 20) % 10] || s[v] || s[0]); };\n\nconst SOCIAL_ICONS = {\n  discord: '<svg viewBox=\"0 0 24 24\" fill=\"currentColor\"><path d=\"M20.3 4.4A19.8 19.8 0 0 0 15.4 3l-.3.5c1.7.4 2.9 1 4 1.7a13.5 13.5 0 0 0-11.4 0c1.1-.7 2.5-1.4 4.1-1.7L11.6 3A19.8 19.8 0 0 0 6.7 4.4C3.6 9 2.8 13.5 3.2 17.9a19.9 19.9 0 0 0 6 3l.8-1.3c-.7-.3-1.4-.6-2-1l.5-.4a14.2 14.2 0 0 0 12.2 0l.5.4c-.6.4-1.3.7-2 1l.8 1.3a19.8 19.8 0 0 0 6-3c.5-5.1-.8-9.6-3.6-13.5ZM9.5 15.3c-1 0-1.8-.9-1.8-2s.8-2 1.8-2 1.8.9 1.8 2-.8 2-1.8 2Zm5 0c-1 0-1.8-.9-1.8-2s.8-2 1.8-2 1.8.9 1.8 2-.8 2-1.8 2Z\"/></svg>',\n  kick: '<svg viewBox=\"0 0 24 24\" fill=\"currentColor\"><path d=\"M3 3h5v5h2V6h2V4h2V3h4v6h-2v2h-2v2h2v2h2v6h-4v-1h-2v-2h-2v-2H8v5H3V3Z\"/></svg>',\n  twitch: '<svg viewBox=\"0 0 24 24\" fill=\"currentColor\"><path d=\"M4 2 3 6v13h4v3h3l3-3h4l5-5V2H4Zm16 10-3 3h-4l-3 3v-3H7V4h13v8Zm-3-6h-2v5h2V6Zm-5 0h-2v5h2V6Z\"/></svg>',\n  x: '<svg viewBox=\"0 0 24 24\" fill=\"currentColor\"><path d=\"M18.9 2h3.3l-7.2 8.3L23.5 22h-6.6l-5.2-6.8L5.7 22H2.4l7.7-8.8L1.9 2h6.8l4.7 6.2L18.9 2Zm-1.2 18h1.8L7.1 3.9H5.2L17.7 20Z\"/></svg>',\n};\n\nfunction boot() {\n  const data = window.__SITE_DATA__ || {};\n  const b = data.brand || {};\n  $$(\"[data-brand-name]\").forEach((el) => (el.textContent = b.name || \"\"));\n  $$(\"[data-code]\").forEach((el) => (el.textContent = b.code || \"\"));\n  $$(\"[data-casino]\").forEach((el) => (el.textContent = b.casino || \"Stake\"));\n  $$(\"[data-pool]\").forEach((el) => (el.textContent = b.prizePool || \"\"));\n  $$(\"[data-period]\").forEach((el) => (el.textContent = b.period || \"Monthly\"));\n  const tg = $(\"[data-tagline]\"); if (tg) tg.textContent = b.tagline || \"\";\n  const yr = $(\"[data-year]\"); if (yr) yr.textContent = new Date().getFullYear();\n  // SSR already points [data-cta] at the tracked /go/<slug> redirect; only fall back to the raw URL when no slug is set.\n  $$(\"[data-cta]\").forEach((el) => { if (b.ctaUrl && !window.__SLUG__) el.href = b.ctaUrl; });\n\n  const wm = $(\"[data-watermarks]\");\n  if (wm) { let h = \"\"; for (let i = 0; i < 14; i++) { const t = Math.random()*100, l = Math.random()*100, s = 20+Math.random()*60, r = -20+Math.random()*40; h += `<span style=\"top:${t}%;left:${l}%;font-size:${s}px;transform:rotate(${r}deg)\">${esc(b.name || \"\")}</span>`; } wm.innerHTML = h; }\n\n  const cc = $(\"[data-copy-code]\");\n  if (cc) cc.addEventListener(\"click\", async () => { try { await navigator.clipboard.writeText(b.code || \"\"); cc.classList.add(\"copied\"); const p = cc.textContent; cc.textContent = \"Copied!\"; setTimeout(() => { cc.classList.remove(\"copied\"); cc.textContent = p; }, 1300); } catch (_) {}\n    try { if (window.__SLUG__) navigator.sendBeacon(\"/api/track/copy\", new Blob([JSON.stringify({ slug: window.__SLUG__ })], { type: \"application/json\" })); } catch (_) {} });\n\n  const p = data.partner || {};\n  const blurb = $(\"[data-partner-blurb]\"); if (blurb) blurb.textContent = p.blurb || \"\";\n  const chips = $(\"[data-chips]\"); if (chips && Array.isArray(p.chips)) chips.innerHTML = p.chips.map((c) => `<li>${esc(c)}</li>`).join(\"\");\n\n  const why = $(\"[data-why]\");\n  if (why && Array.isArray(data.whyStats)) why.innerHTML = data.whyStats.map((s) => `<div class=\"why\"><span class=\"why-big\">${esc(s.big)}</span><span class=\"why-label\">${esc(s.label)}</span>${s.sub ? `<span class=\"why-sub\">${esc(s.sub)}</span>` : \"\"}</div>`).join(\"\");\n\n  const players = (data.players || []).slice().sort((a, b) => b.wagered - a.wagered);\n  const cnt = $(\"[data-count]\"); if (cnt) cnt.textContent = players.length;\n\n  const t3 = $(\"[data-top3]\");\n  if (t3 && players.length >= 3) t3.innerHTML = players.slice(0, 3).map((pl, i) => { const r = i + 1; return `<div class=\"t3 t3--${r}\"><span class=\"t3-medal\">RANK ${String(r).padStart(2,\"0\")}</span><div class=\"t3-name\">${esc(pl.name)}</div><div class=\"t3-wager\">${money(pl.wagered)}</div><span class=\"t3-prize\">${pl.prize ? moneyShort(pl.prize) : \"—\"}</span></div>`; }).join(\"\");\n\n  const rows = $(\"[data-rows]\");\n  if (rows) rows.innerHTML = players.map((pl, i) => { const r = i + 1; const prize = pl.prize ? `<span class=\"tr-prize has ta-r\">${moneyShort(pl.prize)}</span>` : `<span class=\"tr-prize no ta-r\">—</span>`; return `<li class=\"t-row\" style=\"animation-delay:${Math.min(i*0.025,0.5)}s\"><span class=\"tr-rank\">${String(r).padStart(2,\"0\")}</span><span class=\"tr-player\"><span class=\"tr-av\">${esc(initials(pl.name))}</span><span class=\"tr-name\">${esc(pl.name)}</span></span><span class=\"tr-wager\">${money(pl.wagered)}</span>${prize}</li>`; }).join(\"\");\n\n  const rl = $(\"[data-rules]\"); if (rl && Array.isArray(data.rules)) rl.innerHTML = data.rules.map((r) => `<li>${esc(r)}</li>`).join(\"\");\n\n  // Prize breakdown — derived from the players' prize column, equal prizes grouped into rank ranges.\n  const po = $(\"[data-payouts]\");\n  if (po) {\n    const winners = players.map((pl, idx) => ({ rank: idx + 1, prize: pl.prize })).filter((w) => w.prize > 0);\n    if (winners.length) {\n      const groups = [];\n      winners.forEach((w) => {\n        const last = groups[groups.length - 1];\n        if (last && last.prize === w.prize && last.to === w.rank - 1) last.to = w.rank;\n        else groups.push({ from: w.rank, to: w.rank, prize: w.prize });\n      });\n      po.innerHTML = `<span class=\"pay-label\">Payouts</span>` + groups.map((g) =>\n        `<span class=\"pay\"><b>${g.from === g.to ? ord(g.from) : `${ord(g.from)}–${ord(g.to)}`}</b>${moneyShort(g.prize)}</span>`).join(\"\");\n      po.hidden = false;\n    }\n  }\n\n  // Past winners — closed-out periods, newest first.\n  const pw = Array.isArray(data.pastWinners) ? data.pastWinners : [];\n  const pastSec = $(\"[data-past]\"), pastGrid = $(\"[data-past-grid]\");\n  if (pastSec && pastGrid && pw.length) {\n    const medals = [\"gold\", \"silver\", \"bronze\"];\n    pastGrid.innerHTML = pw.map((a) => `<div class=\"past-card\"><div class=\"past-label\">${esc(a.label)}</div><ol class=\"past-list\">${\n      (a.top || []).map((p, i) => `<li class=\"past-row\"><span class=\"past-rank ${medals[i] || \"\"}\">${i + 1}</span><span class=\"past-name\">${esc(p.name)}</span><span class=\"past-val\">${p.prize ? moneyShort(p.prize) : money(p.wagered)}</span></li>`).join(\"\")\n    }</ol></div>`).join(\"\");\n    pastSec.hidden = false;\n  }\n\n  const sc = $(\"[data-socials]\");\n  if (sc && Array.isArray(data.socials)) sc.innerHTML = data.socials.map((s) => { const brand = (s.brand || s.name || \"\").toLowerCase(); const ico = SOCIAL_ICONS[brand] || SOCIAL_ICONS.discord; return `<div class=\"scard\"><div class=\"scard-ico ${esc(brand)}\">${ico}</div><div class=\"scard-name\">${esc(s.name)}</div><div class=\"scard-handle\">${esc(s.handle || \"\")}</div><a class=\"btn btn--grad\" href=\"${safeUrl(s.url)}\" target=\"_blank\" rel=\"noopener\">${esc(s.action || \"Follow\")}</a></div>`; }).join(\"\");\n\n  startCountdown(data.endsAt);\n}\n\nfunction startCountdown(endsAt) {\n  const el = $(\"[data-countdown]\");\n  const grid = $(\"[data-timer-grid]\");\n  const cell = (k) => (grid ? grid.querySelector(`[data-t=\"${k}\"]`) : null);\n  const cells = grid ? { d: cell(\"d\"), h: cell(\"h\"), m: cell(\"m\"), s: cell(\"s\") } : null;\n  if (!el && !cells) return;\n  const target = endsAt ? new Date(endsAt).getTime() : nextMonthUTC();\n  const pad = (n) => String(n).padStart(2, \"0\");\n  const tick = () => {\n    const diff = target - Date.now();\n    if (diff <= 0) { if (el) el.textContent = \"Resetting\"; if (cells) Object.values(cells).forEach((c) => c && (c.textContent = \"00\")); return; }\n    const d = Math.floor(diff / 86400000), h = Math.floor((diff % 86400000) / 3600000), m = Math.floor((diff % 3600000) / 60000), s = Math.floor((diff % 60000) / 1000);\n    if (el) el.textContent = `${d}d ${pad(h)}h ${pad(m)}m`;\n    if (cells) { cells.d && (cells.d.textContent = pad(d)); cells.h && (cells.h.textContent = pad(h)); cells.m && (cells.m.textContent = pad(m)); cells.s && (cells.s.textContent = pad(s)); }\n  };\n  tick(); setInterval(tick, 1000);\n}\nfunction nextMonthUTC() { const n = new Date(); return Date.UTC(n.getUTCFullYear(), n.getUTCMonth() + 1, 1); }\ndocument.addEventListener(\"DOMContentLoaded\", boot);\n";
export const landing_js = "document.getElementById(\"yr\").textContent = new Date().getFullYear();\nconst form = document.getElementById(\"leadForm\"), status = document.getElementById(\"l_status\"), submit = document.getElementById(\"l_submit\");\nform.addEventListener(\"submit\", async (e) => {\n  e.preventDefault(); status.textContent = \"\";\n  const payload = { handle: document.getElementById(\"l_handle\").value.trim(), casino: document.getElementById(\"l_casino\").value.trim(), contact: document.getElementById(\"l_contact\").value.trim(), note: document.getElementById(\"l_note\").value.trim() };\n  if (!payload.handle || !payload.contact) { status.textContent = \"Add your handle and how to reach you.\"; return; }\n  submit.disabled = true; submit.textContent = \"Sending…\";\n  try {\n    const res = await fetch(\"/api/lead\", { method: \"POST\", headers: { \"content-type\": \"application/json\" }, body: JSON.stringify(payload) });\n    const d = await res.json();\n    if (res.ok && d.ok) { form.reset(); status.textContent = \"Got it. We'll be in touch soon.\"; } else { status.textContent = d.error || \"Something went wrong.\"; }\n  } catch { status.textContent = \"Network error. Try again.\"; }\n  submit.disabled = false; submit.textContent = \"Send request\";\n});\n";
export const app_css = "/* RankUp app shell — login, signup, dashboard. Monochrome, industrial, one accent. */\n:root{--bg:#0b0b0c;--panel:#0f0f11;--panel-2:#141417;--line:#232327;--line-2:#2e2e33;--ink:#ededf0;--ink-soft:#a3a3ab;--ink-mute:#6a6a72;--accent:#c8ff00;--accent-ink:#0b0b0c;--danger:#ff5c5c;--ok:#57d38c;--mono:\"JetBrains Mono\",ui-monospace,SFMono-Regular,Menlo,monospace;--sans:\"Inter\",system-ui,-apple-system,Segoe UI,Roboto,sans-serif}\n*{box-sizing:border-box}html,body{margin:0}\nbody{background:var(--bg);color:var(--ink);font-family:var(--sans);font-size:15px;line-height:1.5;-webkit-font-smoothing:antialiased}\na{color:var(--ink);text-decoration:none}a:hover{color:var(--accent)}\n::selection{background:var(--accent);color:var(--accent-ink)}\n.mono{font-family:var(--mono)}.muted{color:var(--ink-mute)}\n.label{font-family:var(--mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:var(--ink-mute)}\n.topbar{display:flex;align-items:center;justify-content:space-between;padding:14px 22px;border-bottom:1px solid var(--line)}\n.brand{font-weight:800;letter-spacing:-.02em;font-size:17px}.brand b{color:var(--accent)}\n.topbar-right{display:flex;gap:18px;align-items:center;font-size:13px}\n.btn{display:inline-flex;align-items:center;gap:8px;font-weight:600;font-size:14px;padding:10px 16px;border-radius:8px;border:1px solid var(--line-2);background:var(--panel-2);color:var(--ink);cursor:pointer;transition:border-color .15s,background .15s,transform .05s}\n.btn:hover{border-color:var(--ink-mute)}.btn:active{transform:translateY(1px)}.btn:disabled{opacity:.45;cursor:not-allowed}\n.btn--accent{background:var(--accent);color:var(--accent-ink);border-color:var(--accent)}.btn--accent:hover{filter:brightness(1.05)}\n.btn--ghost{background:transparent}.btn--sm{padding:6px 11px;font-size:13px}.btn--danger{color:var(--danger);border-color:#3a2222}\n.field{display:flex;flex-direction:column;gap:6px;margin-bottom:16px}\n.field label{font-size:13px;color:var(--ink-soft)}\n.field input,.field select,.field textarea{font-family:var(--sans);font-size:14px;color:var(--ink);background:var(--panel);border:1px solid var(--line-2);border-radius:8px;padding:10px 12px;width:100%}\n.field input:focus,.field select:focus,.field textarea:focus{outline:none;border-color:var(--accent)}\n.field .hint{font-size:12px;color:var(--ink-mute)}\n.err{color:var(--danger);font-size:13px;min-height:18px;margin:4px 0}\n.auth-wrap{min-height:100vh;display:grid;grid-template-columns:1.1fr .9fr}\n.auth-side{border-right:1px solid var(--line);padding:48px 44px;display:flex;flex-direction:column;justify-content:space-between}\n.auth-side h1{font-size:34px;line-height:1.1;letter-spacing:-.03em;margin:0 0 12px;max-width:14ch}\n.auth-side p{color:var(--ink-soft);max-width:42ch}\n.auth-side .feat{border-top:1px solid var(--line);padding-top:18px;font-size:13px;color:var(--ink-soft)}\n.auth-side .feat div{padding:5px 0}\n.auth-main{display:flex;align-items:center;justify-content:center;padding:32px}\n.auth-card{width:100%;max-width:360px}\n.auth-card h2{font-size:22px;margin:0 0 4px;letter-spacing:-.01em}.auth-card .sub{color:var(--ink-mute);font-size:13px;margin-bottom:26px}\n.auth-card .foot{margin-top:18px;font-size:13px;color:var(--ink-mute)}\n@media(max-width:820px){.auth-wrap{grid-template-columns:1fr}.auth-side{display:none}}\n.wrap{max-width:1000px;margin:0 auto;padding:26px 22px 80px}\n.dash-head{display:flex;align-items:flex-end;justify-content:space-between;gap:16px;flex-wrap:wrap;margin-bottom:6px}\n.dash-head h1{font-size:24px;letter-spacing:-.02em;margin:0}\n.live-link{font-family:var(--mono);font-size:13px;color:var(--ink-soft)}.live-link a{color:var(--accent)}\n.card{border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:20px;margin-top:18px}\n.card h3{margin:0 0 2px;font-size:15px}.card .card-sub{color:var(--ink-mute);font-size:13px;margin:0 0 16px}\n.grid2{display:grid;grid-template-columns:1fr 1fr;gap:0 20px}\n@media(max-width:640px){.grid2{grid-template-columns:1fr}}\ntable.players{width:100%;border-collapse:collapse;font-size:14px}\ntable.players th{text-align:left;font-family:var(--mono);font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-mute);padding:8px;border-bottom:1px solid var(--line)}\ntable.players td{padding:6px 8px;border-bottom:1px solid var(--line)}\ntable.players td input{background:var(--panel-2);border:1px solid var(--line);border-radius:6px;padding:7px 9px;font-size:13px;color:var(--ink);width:100%}\ntable.players td input:focus{outline:none;border-color:var(--accent)}\ntable.players td.num input{font-family:var(--mono);text-align:right}\ntable.players td.rank{font-family:var(--mono);color:var(--ink-mute);width:34px}\ntable.players td.act{width:34px;text-align:center}\n.row-x{color:var(--ink-mute);cursor:pointer;border:none;background:none;font-size:16px;line-height:1}.row-x:hover{color:var(--danger)}\n.empty{color:var(--ink-mute);font-size:13px;padding:14px 8px;text-align:center;border:1px dashed var(--line-2);border-radius:8px}\n.savebar{position:sticky;bottom:0;margin-top:22px;padding:14px 0;background:linear-gradient(180deg,transparent,var(--bg) 40%);display:flex;align-items:center;gap:14px;justify-content:flex-end}\n.savebar .status{font-size:13px;color:var(--ink-mute);margin-right:auto}\n.skel{color:var(--ink-mute);padding:40px;text-align:center}\n\n/* --- plan & billing --- */\n.plan-row{display:flex;align-items:center;justify-content:space-between;gap:16px;border:1px solid var(--line-2);border-radius:10px;padding:14px 16px;background:var(--panel-2)}\n.plan-name{font-weight:800;font-size:17px;letter-spacing:-.01em}\n.msg{color:var(--ok);font-size:13px;margin:4px 0}\n.hint{font-size:12px;color:var(--ink-mute)}\n\n/* --- admin --- */\n.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin:18px 0 6px}\n.stat{border:1px solid var(--line);border-radius:10px;background:var(--panel);padding:14px 16px}\n.stat b{display:block;font-size:22px;font-family:var(--mono);letter-spacing:-.02em}\n.stat span{font-size:12px;color:var(--ink-mute)}\n@media(max-width:700px){.stats{grid-template-columns:1fr 1fr}}\n.tabs{display:flex;gap:2px;border-bottom:1px solid var(--line);margin-top:20px}\n.tab{background:none;border:none;color:var(--ink-mute);font-family:var(--mono);font-size:12px;letter-spacing:.08em;text-transform:uppercase;padding:10px 14px;cursor:pointer;border-bottom:2px solid transparent}\n.tab:hover{color:var(--ink)}\n.tab.is-on{color:var(--accent);border-bottom-color:var(--accent)}\n.tabpane{margin-top:6px;overflow-x:auto}\ntable.admin-table{width:100%;border-collapse:collapse;font-size:13px;min-width:720px}\ntable.admin-table th{text-align:left;font-family:var(--mono);font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-mute);padding:10px 8px;border-bottom:1px solid var(--line)}\ntable.admin-table td{padding:9px 8px;border-bottom:1px solid var(--line);vertical-align:top}\ntable.admin-table td a{color:var(--accent)}\ntable.admin-table td.note{max-width:260px;color:var(--ink-soft)}\n.ta-r{text-align:right}\n.actions{display:flex;gap:6px;flex-wrap:wrap}\n.btn--xs{padding:4px 8px;font-size:12px;border-radius:6px}\n.pill{display:inline-block;font-family:var(--mono);font-size:11px;padding:2px 8px;border-radius:99px;border:1px solid var(--line-2)}\n.pill--good{color:var(--ok);border-color:#1e3a2b}\n.pill--bad{color:var(--danger);border-color:#3a2222}\n.pill--muted{color:var(--ink-mute)}\n.pill--info{color:var(--accent);border-color:#3a4218}\n\n/* --- legal pages --- */\n.legal{max-width:680px;margin:0 auto;padding:40px 22px 80px}\n.legal h1{font-size:28px;letter-spacing:-.02em;margin:0 0 4px}\n.legal-updated{font-family:var(--mono);font-size:12px;color:var(--ink-mute);margin:0 0 30px}\n.legal h2{font-size:16px;margin:28px 0 8px;letter-spacing:-.01em}\n.legal p,.legal li{color:var(--ink-soft);font-size:14px;line-height:1.65}\n.legal ul{padding-left:20px;margin:8px 0}\n.legal li{margin:6px 0}\n.legal a{color:var(--accent)}\n.legal b{color:var(--ink)}\n.legal-foot{border-top:1px solid var(--line);margin-top:40px;padding-top:16px;font-size:13px}\n.legal-foot a{color:var(--ink-soft)}.legal-foot a:hover{color:var(--accent)}\n.topbar a.brand{color:var(--ink)}\n\n/* --- paste / import --- */\n.import{margin-top:12px;border:1px solid var(--line-2);border-radius:10px;padding:14px;background:var(--panel-2)}\n.import textarea{width:100%;font-family:var(--mono);font-size:13px;color:var(--ink);background:var(--panel);border:1px solid var(--line-2);border-radius:8px;padding:10px 12px;resize:vertical}\n.import textarea:focus{outline:none;border-color:var(--accent)}\n.import-foot{display:flex;align-items:center;gap:14px;margin-top:10px;flex-wrap:wrap}\n.import-foot .btn{margin-left:auto}\n.chk{display:flex;gap:6px;align-items:center;cursor:pointer}\n\n/* --- branding --- */\n.logo-row{display:flex;align-items:center;gap:10px;flex-wrap:wrap}\n.logo-preview{width:44px;height:44px;object-fit:contain;border-radius:8px;border:1px solid var(--line-2);background:var(--panel-2)}\n.color-row{display:flex;align-items:center;gap:8px}\n.color-row input[type=color]{width:44px;height:34px;padding:2px;border:1px solid var(--line-2);border-radius:8px;background:var(--panel);cursor:pointer}\n\n/* --- past winners / archives --- */\n.arch-form{display:flex;gap:12px;align-items:flex-end;flex-wrap:wrap;margin-bottom:14px}\n.arch-form .field select{min-width:200px}\n.arch-list{display:flex;flex-direction:column;gap:6px}\n.arch-row{display:flex;align-items:center;gap:12px;border:1px solid var(--line);border-radius:8px;padding:9px 12px;background:var(--panel-2);font-size:13px}\n.arch-label{font-weight:600}\n.arch-row .hint{margin-left:auto}\n.arch-del{flex:none}\n\n/* Analytics card */\n.stat-tiles{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:14px}\n.stat-tile{border:1px solid var(--line);border-radius:10px;background:var(--panel-2);padding:12px 14px;display:flex;flex-direction:column;gap:2px}\n.stat-num{font-family:var(--mono);font-size:22px;font-weight:700;letter-spacing:-.02em}\n.stat-lbl{font-family:var(--mono);font-size:10px;letter-spacing:.12em;text-transform:uppercase;color:var(--ink-mute)}\n.stat-chart{border:1px solid var(--line);border-radius:10px;background:var(--panel-2);padding:12px 14px 8px}\n.stat-bars{display:flex;align-items:flex-end;gap:4px;height:64px}\n.stat-bar{flex:1;min-width:4px;background:var(--accent);opacity:.85;border-radius:2px 2px 0 0;transition:opacity .15s}\n.stat-bar:hover{opacity:1}\n.stat-chart-lbl{display:flex;justify-content:space-between;margin-top:6px;font-family:var(--mono);font-size:10px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-mute)}\n@media (max-width:640px){.stat-tiles{grid-template-columns:repeat(2,1fr)}}\n";
export const dashboard_js = "/* Dashboard: load the user's site, edit brand + players, save back, manage plan. */\nconst $ = (id) => document.getElementById(id);\nlet SLUG = null, EXTRA = {}, ME = null;\nlet LOGO; // undefined = unchanged, null = remove, string = new data URI\nconst DEFAULT_A = \"#5ad9ff\", DEFAULT_B = \"#7b8cff\";\nfunction toLocalInput(iso){ if(!iso) return \"\"; const d=new Date(iso); if(isNaN(d)) return \"\"; const p=(n)=>String(n).padStart(2,\"0\"); return `${d.getUTCFullYear()}-${p(d.getUTCMonth()+1)}-${p(d.getUTCDate())}T${p(d.getUTCHours())}:${p(d.getUTCMinutes())}`; }\nfunction fromLocalInput(v){ if(!v) return \"\"; return v.length===16 ? `${v}:00Z` : new Date(v).toISOString(); }\nasync function init(){\n  let me; try { me = await (await fetch(\"/api/auth/me\")).json(); } catch { me=null; }\n  if (!me || !me.ok || !me.user) { location.href=\"/login\"; return; }\n  ME = me.user;\n  $(\"userEmail\").textContent = ME.email;\n  if (ME.isAdmin) $(\"adminLink\").hidden = false;\n  renderPlan();\n  const res = await fetch(\"/api/site\"); const p = await res.json();\n  if (!p.ok) {\n    if (ME.isAdmin) { location.href = \"/admin\"; return; }\n    $(\"loading\").textContent = \"Couldn't load your site.\"; return;\n  }\n  SLUG = p.slug; const d = p.data||{}; const b = d.brand||{};\n  loadStats(); // non-blocking; fills the analytics card when it lands\n  EXTRA = { chips: d.partner?.chips, whyStats: d.whyStats, rules: d.rules, socials: d.socials };\n  $(\"f_name\").value=b.name||\"\"; $(\"f_tagline\").value=b.tagline||\"\"; $(\"f_casino\").value=b.casino||\"Stake\";\n  $(\"f_code\").value=b.code||\"\"; $(\"f_cta\").value=b.ctaUrl||\"\"; $(\"f_pool\").value=b.prizePool||\"\";\n  $(\"f_period\").value=b.period||\"Monthly\"; $(\"f_ends\").value=toLocalInput(d.endsAt); $(\"f_blurb\").value=d.partner?.blurb||\"\";\n  renderPlayers(d.players||[]);\n  renderBranding(d.branding||{});\n  renderArchives(p.archives||[]);\n  $(\"a_label\").placeholder = new Date().toLocaleString(\"en-US\",{month:\"long\",year:\"numeric\",timeZone:\"UTC\"});\n  $(\"liveLink\").textContent = location.host + \"/\" + SLUG; $(\"liveLink\").href = \"/\" + SLUG; $(\"viewLive\").href = \"/\" + SLUG;\n  $(\"loading\").hidden=true; $(\"dash\").hidden=false;\n  if (new URLSearchParams(location.search).get(\"upgraded\")) {\n    $(\"status\").textContent = \"Payment received — Pro activates once the network confirms (usually minutes).\";\n  }\n}\nfunction renderPlan(){\n  const pro = ME.plan === \"pro\";\n  $(\"planBadge\").textContent = (ME.plan||\"free\").toUpperCase() + \" PLAN\";\n  $(\"planName\").textContent = pro ? \"Pro\" : \"Free\";\n  if (pro) {\n    const until = ME.planExpiresAt ? new Date(ME.planExpiresAt).toLocaleDateString() : null;\n    $(\"planMeta\").textContent = until ? `Active until ${until} · up to ${ME.limits.players} players · no badge` : `Lifetime · up to ${ME.limits.players} players · no badge`;\n    $(\"goPro\").textContent = \"Extend Pro (+31 days)\";\n  } else {\n    $(\"planMeta\").textContent = `Up to ${ME.limits.players} players · RankUp badge on your page`;\n    $(\"goPro\").textContent = `Upgrade to Pro — $${ME.proPrice}/mo`;\n  }\n}\nasync function checkout(btn){\n  btn.disabled = true; const orig = btn.textContent; btn.textContent = \"Opening checkout…\";\n  try {\n    const res = await fetch(\"/api/billing/checkout\", { method: \"POST\" });\n    const d = await res.json();\n    if (res.ok && d.ok && d.url) { location.href = d.url; return; }\n    $(\"status\").textContent = d.error || \"Couldn't start checkout.\";\n  } catch { $(\"status\").textContent = \"Network error.\"; }\n  btn.disabled = false; btn.textContent = orig;\n}\nfunction playerRow(p={name:\"\",wagered:\"\",prize:\"\"}){\n  const esc=(s)=>String(s??\"\").replace(/[&<>\"']/g,(c)=>({\"&\":\"&amp;\",\"<\":\"&lt;\",\">\":\"&gt;\",\"\\\"\":\"&quot;\",\"'\":\"&#39;\"}[c]));\n  const tr=document.createElement(\"tr\");\n  tr.innerHTML = `<td class=\"rank\"></td><td><input class=\"p-name\" placeholder=\"*****ess\" value=\"${esc(p.name)}\"></td><td class=\"num\"><input class=\"p-wager\" inputmode=\"decimal\" placeholder=\"0\" value=\"${esc(p.wagered)}\"></td><td class=\"num\"><input class=\"p-prize\" inputmode=\"decimal\" placeholder=\"0\" value=\"${esc(p.prize)}\"></td><td class=\"act\"><button class=\"row-x\" title=\"Remove\" type=\"button\">×</button></td>`;\n  tr.querySelector(\".row-x\").addEventListener(\"click\",()=>{tr.remove();renumber();toggleEmpty();});\n  return tr;\n}\nfunction renderPlayers(list){ const b=$(\"rows\"); b.innerHTML=\"\"; list.forEach(p=>b.appendChild(playerRow(p))); renumber(); toggleEmpty(); }\nfunction renumber(){\n  const rows=[...$(\"rows\").children];\n  rows.forEach((tr,i)=>tr.querySelector(\".rank\").textContent=String(i+1).padStart(2,\"0\"));\n  if (ME) $(\"pCount\").textContent = `${rows.length} / ${ME.limits.players}`;\n}\nfunction toggleEmpty(){ $(\"playersEmpty\").hidden=$(\"rows\").children.length>0; }\n$(\"addRow\").addEventListener(\"click\",()=>{\n  if (ME && $(\"rows\").children.length >= ME.limits.players) {\n    $(\"status\").textContent = ME.plan===\"pro\" ? `Pro allows up to ${ME.limits.players} players.` : `Free plan allows ${ME.limits.players} players. Upgrade to Pro for 50.`;\n    setTimeout(()=>$(\"status\").textContent=\"\",5000); return;\n  }\n  $(\"rows\").appendChild(playerRow());renumber();toggleEmpty();\n});\nfunction collect(){\n  const players=[...$(\"rows\").children].map(tr=>({name:tr.querySelector(\".p-name\").value.trim(),wagered:parseFloat(tr.querySelector(\".p-wager\").value)||0,prize:parseFloat(tr.querySelector(\".p-prize\").value)||0})).filter(p=>p.name);\n  const out = { brand:{name:$(\"f_name\").value.trim(),tagline:$(\"f_tagline\").value.trim(),casino:$(\"f_casino\").value.trim()||\"Stake\",code:$(\"f_code\").value.trim(),ctaUrl:$(\"f_cta\").value.trim(),prizePool:$(\"f_pool\").value.trim(),period:$(\"f_period\").value.trim()||\"Monthly\"}, endsAt:fromLocalInput($(\"f_ends\").value), partner:{blurb:$(\"f_blurb\").value.trim(),chips:EXTRA.chips}, whyStats:EXTRA.whyStats, rules:EXTRA.rules, socials:EXTRA.socials, players };\n  if (ME && ME.plan === \"pro\") {\n    out.branding = { accentA: $(\"c_a\").value, accentB: $(\"c_b\").value };\n    if (LOGO !== undefined) out.branding.logo = LOGO;\n  }\n  return out;\n}\n\n/* --- branding (Pro) --- */\nfunction renderBranding(br){\n  const pro = ME.plan === \"pro\";\n  $(\"brandBody\").hidden = !pro; $(\"brandLock\").hidden = pro;\n  if (br.accentA) $(\"c_a\").value = br.accentA;\n  if (br.accentB) $(\"c_b\").value = br.accentB;\n  if (br.hasLogo) { $(\"logoPreview\").src = \"/logo/\" + SLUG + \"?t=\" + Date.now(); $(\"logoPreview\").hidden = false; $(\"logoClear\").hidden = false; }\n}\n$(\"logoPick\").addEventListener(\"click\",()=>$(\"logoFile\").click());\n$(\"logoClear\").addEventListener(\"click\",()=>{ LOGO = null; $(\"logoPreview\").hidden = true; $(\"logoClear\").hidden = true; $(\"status\").textContent = \"Logo will be removed when you save.\"; });\n$(\"logoFile\").addEventListener(\"change\",()=>{\n  const f = $(\"logoFile\").files[0]; if (!f) return;\n  const img = new Image();\n  img.onload = ()=>{\n    const max = 512, scale = Math.min(1, max / Math.max(img.width, img.height));\n    const c = document.createElement(\"canvas\");\n    c.width = Math.max(1, Math.round(img.width * scale)); c.height = Math.max(1, Math.round(img.height * scale));\n    c.getContext(\"2d\").drawImage(img, 0, 0, c.width, c.height);\n    let uri = c.toDataURL(\"image/png\");\n    if (uri.length > 240000) uri = c.toDataURL(\"image/jpeg\", 0.85);\n    if (uri.length > 240000) { $(\"status\").textContent = \"That image is too big even after resizing. Try a simpler one.\"; return; }\n    LOGO = uri;\n    $(\"logoPreview\").src = uri; $(\"logoPreview\").hidden = false; $(\"logoClear\").hidden = false;\n    $(\"status\").textContent = \"Logo ready — hit Save to publish it.\";\n    URL.revokeObjectURL(img.src);\n  };\n  img.onerror = ()=>{ $(\"status\").textContent = \"Couldn't read that image.\"; };\n  img.src = URL.createObjectURL(f);\n  $(\"logoFile\").value = \"\";\n});\n$(\"colorsReset\").addEventListener(\"click\",()=>{ $(\"c_a\").value = DEFAULT_A; $(\"c_b\").value = DEFAULT_B; $(\"status\").textContent = \"Colors reset — hit Save to apply.\"; });\n$(\"brandUpgrade\").addEventListener(\"click\",(e)=>{ e.preventDefault(); checkout($(\"goPro\")); });\n\n/* --- paste / import --- */\nfunction parseImport(text){\n  const num = (s)=>parseFloat(String(s||\"\").replace(/[$,\\s]/g,\"\"))||0;\n  return String(text||\"\").split(/\\r?\\n/).map(l=>l.trim()).filter(Boolean).map(l=>{\n    const parts = l.split(/\\t|,|;/).map(s=>s.trim()).filter(s=>s!==\"\");\n    if (!parts.length || !parts[0]) return null;\n    return { name: parts[0].slice(0,40), wagered: num(parts[1]), prize: num(parts[2]) };\n  }).filter(Boolean);\n}\n$(\"importBtn\").addEventListener(\"click\",()=>{ const p=$(\"importPanel\"); p.hidden=!p.hidden; if(!p.hidden) $(\"importText\").focus(); });\n$(\"importText\").addEventListener(\"input\",()=>{\n  const n = parseImport($(\"importText\").value).length;\n  $(\"importPreview\").textContent = n + (n===1?\" player\":\" players\") + \" detected\";\n  $(\"importApply\").disabled = n===0;\n});\n$(\"importApply\").addEventListener(\"click\",()=>{\n  let parsed = parseImport($(\"importText\").value);\n  if (!parsed.length) return;\n  const replace = $(\"importReplace\").checked;\n  const existing = replace ? [] : [...$(\"rows\").children].map(tr=>({name:tr.querySelector(\".p-name\").value.trim(),wagered:parseFloat(tr.querySelector(\".p-wager\").value)||0,prize:parseFloat(tr.querySelector(\".p-prize\").value)||0})).filter(p=>p.name);\n  let all = existing.concat(parsed);\n  let note = \"\";\n  if (ME && all.length > ME.limits.players) { note = ` (cut to your ${ME.limits.players}-player limit)`; all = all.slice(0, ME.limits.players); }\n  renderPlayers(all);\n  $(\"importText\").value = \"\"; $(\"importPreview\").textContent = \"0 players detected\"; $(\"importApply\").disabled = true; $(\"importPanel\").hidden = true;\n  $(\"status\").textContent = `${parsed.length} imported${note} — hit Save to publish.`;\n});\n\n/* --- past winners / close out --- */\nfunction renderArchives(list){\n  const box = $(\"archList\"); box.innerHTML = \"\";\n  $(\"archEmpty\").hidden = list.length > 0;\n  list.forEach(a=>{\n    const row = document.createElement(\"div\"); row.className = \"arch-row\";\n    const when = new Date(a.at).toLocaleDateString();\n    row.innerHTML = `<span class=\"arch-label\"></span><span class=\"hint\">${a.players} players · closed ${when}</span><button class=\"btn btn--xs btn--ghost arch-del\" type=\"button\">Delete</button>`;\n    row.querySelector(\".arch-label\").textContent = a.label;\n    row.querySelector(\".arch-del\").addEventListener(\"click\", async ()=>{\n      if (!confirm(`Delete the \"${a.label}\" archive? It disappears from your page too.`)) return;\n      const res = await fetch(\"/api/site/archive/delete\",{method:\"POST\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify({id:a.id})});\n      const d = await res.json();\n      if (res.ok && d.ok) { row.remove(); if(!$(\"archList\").children.length) $(\"archEmpty\").hidden=false; $(\"status\").textContent=\"Archive deleted.\"; }\n      else $(\"status\").textContent = d.error || \"Couldn't delete that.\";\n    });\n    box.appendChild(row);\n  });\n}\n$(\"a_go\").addEventListener(\"click\", async ()=>{\n  const btn = $(\"a_go\"), status = $(\"status\");\n  if (![...$(\"rows\").children].length) { status.textContent = \"The board is empty — nothing to close out.\"; return; }\n  const clear = $(\"a_clear\").value;\n  const warn = clear===\"players\" ? \"save the current board as past winners, then CLEAR the player list\" : clear===\"wagers\" ? \"save the current board as past winners, then reset every wager to 0\" : \"save the current board as past winners\";\n  if (!confirm(`This will ${warn}. Continue?`)) return;\n  btn.disabled = true; btn.textContent = \"Closing out…\";\n  try {\n    // Persist any unsaved edits first so the snapshot matches what's on screen.\n    const saveRes = await fetch(\"/api/site\",{method:\"PUT\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify(collect())});\n    const saved = await saveRes.json();\n    if (!saveRes.ok || !saved.ok) { status.textContent = saved.error || \"Couldn't save before archiving.\"; btn.disabled=false; btn.textContent=\"Close out period\"; return; }\n    const res = await fetch(\"/api/site/archive\",{method:\"POST\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify({label:$(\"a_label\").value.trim(),clear})});\n    const d = await res.json();\n    if (res.ok && d.ok) {\n      const p = await (await fetch(\"/api/site\")).json();\n      if (p.ok) { renderPlayers(p.data.players||[]); renderArchives(p.archives||[]); }\n      $(\"a_label\").value = \"\";\n      status.textContent = `\"${d.label}\" closed out — it's on your page now.`;\n    } else status.textContent = d.error || \"Couldn't close out the period.\";\n  } catch { status.textContent = \"Network error.\"; }\n  btn.disabled = false; btn.textContent = \"Close out period\";\n});\n$(\"save\").addEventListener(\"click\", async ()=>{\n  const btn=$(\"save\"),status=$(\"status\"); btn.disabled=true;btn.textContent=\"Saving…\";status.textContent=\"\";\n  try { const res=await fetch(\"/api/site\",{method:\"PUT\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify(collect())}); const d=await res.json(); if(res.ok&&d.ok) status.textContent=\"Saved. Your page is updated.\"; else status.textContent=d.error||\"Save failed.\"; }\n  catch{ status.textContent=\"Network error.\"; }\n  btn.disabled=false;btn.textContent=\"Save changes\"; setTimeout(()=>status.textContent=\"\",6000);\n});\nasync function loadStats(){\n  let s; try { const r = await fetch(\"/api/site/stats\"); const d = await r.json(); if(!r.ok||!d.ok) return; s = d.stats; } catch { return; }\n  const fmt = (n)=> n>=10000 ? (n/1000).toFixed(1).replace(/\\.0$/,\"\")+\"k\" : String(n);\n  $(\"st_views7\").textContent = fmt(s.last7.views);\n  $(\"st_views30\").textContent = fmt(s.last30.views);\n  $(\"st_copies30\").textContent = fmt(s.last30.copies);\n  $(\"st_clicks30\").textContent = fmt(s.last30.clicks);\n  const bars = $(\"statBars\"); const days = s.days||[];\n  const max = Math.max(1, ...days.map(x=>x.views));\n  bars.innerHTML = days.map(x=>{\n    const h = Math.max(2, Math.round((x.views/max)*100));\n    const nice = new Date(x.day+\"T00:00:00Z\").toUTCString().slice(5,11);\n    return `<div class=\"stat-bar\" style=\"height:${h}%\" title=\"${nice}: ${x.views} views, ${x.copies} copies, ${x.clicks} clicks\"></div>`;\n  }).join(\"\");\n  if (days.length) $(\"statFrom\").textContent = new Date(days[0].day+\"T00:00:00Z\").toUTCString().slice(5,11);\n  if (s.last30.views===0 && s.last30.copies===0 && s.last30.clicks===0) $(\"statsEmpty\").hidden = false;\n}\n\n$(\"logout\").addEventListener(\"click\", async (e)=>{ e.preventDefault(); await fetch(\"/api/auth/logout\",{method:\"POST\"}); location.href=\"/login\"; });\n$(\"upgrade\").addEventListener(\"click\",(e)=>{ e.preventDefault(); checkout($(\"goPro\")); });\n$(\"goPro\").addEventListener(\"click\",()=>checkout($(\"goPro\")));\ninit();\n";
export const admin_js = "/* Operator panel: users, leads, payments, actions. */\nconst $ = (id) => document.getElementById(id);\nconst esc = (s) => String(s ?? \"\").replace(/[&<>\"']/g, (c) => ({ \"&\": \"&amp;\", \"<\": \"&lt;\", \">\": \"&gt;\", '\"': \"&quot;\", \"'\": \"&#39;\" }[c]));\nconst when = (ms) => { if (!ms) return \"–\"; const d = new Date(Number(ms)); return d.toLocaleDateString() + \" \" + d.toLocaleTimeString([], { hour: \"2-digit\", minute: \"2-digit\" }); };\n\nasync function api(path, opts) {\n  const res = await fetch(path, opts);\n  const d = await res.json().catch(() => ({}));\n  if (res.status === 401) { location.href = \"/login\"; throw new Error(\"auth\"); }\n  if (res.status === 403) { document.body.innerHTML = \"<p style='padding:40px;font-family:system-ui'>Not an admin account. <a href='/dashboard'>Back to dashboard</a></p>\"; throw new Error(\"forbidden\"); }\n  return d;\n}\n\nasync function init() {\n  const me = await api(\"/api/auth/me\");\n  if (!me.ok || !me.user) { location.href = \"/login\"; return; }\n  $(\"userEmail\").textContent = me.user.email;\n  const [ov] = await Promise.all([api(\"/api/admin/overview\")]); // 403s here for non-admins\n  $(\"s_users\").textContent = ov.users; $(\"s_pro\").textContent = ov.pro;\n  $(\"s_leads\").textContent = ov.leads; $(\"s_rev\").textContent = \"$\" + Number(ov.revenue || 0).toLocaleString();\n  await Promise.all([loadUsers(), loadLeads(), loadPayments()]);\n  $(\"loading\").hidden = true; $(\"panel\").hidden = false;\n}\n\ndocument.querySelectorAll(\".tab\").forEach((t) => t.addEventListener(\"click\", () => {\n  document.querySelectorAll(\".tab\").forEach((x) => x.classList.toggle(\"is-on\", x === t));\n  document.querySelectorAll(\".tabpane\").forEach((p) => (p.hidden = p.id !== \"tab-\" + t.dataset.tab));\n}));\n\nfunction pill(text, tone) { return `<span class=\"pill pill--${tone}\">${esc(text)}</span>`; }\n\nasync function loadUsers() {\n  const d = await api(\"/api/admin/users\");\n  const rows = d.users || [];\n  $(\"usersEmpty\").hidden = rows.length > 0;\n  $(\"usersBody\").innerHTML = rows.map((u) => {\n    const proLive = u.plan === \"pro\" && (!u.plan_expires_at || Number(u.plan_expires_at) > Date.now());\n    const planTxt = proLive ? (u.plan_expires_at ? \"pro · until \" + new Date(Number(u.plan_expires_at)).toLocaleDateString() : \"pro · lifetime\") : \"free\";\n    return `<tr>\n<td>${esc(u.email)}${u.is_admin ? \" \" + pill(\"admin\", \"info\") : \"\"}</td>\n<td>${u.slug ? `<a href=\"/${esc(u.slug)}\" target=\"_blank\">/${esc(u.slug)}</a>` : \"–\"}</td>\n<td>${pill(planTxt, proLive ? \"good\" : \"muted\")}</td>\n<td>${pill(u.status, u.status === \"active\" ? \"good\" : \"bad\")}</td>\n<td class=\"ta-r\">${u.player_count ?? 0}</td>\n<td>${when(u.created_at)}</td>\n<td class=\"actions\">\n<button class=\"btn btn--xs\" data-act=\"pro\" data-id=\"${u.id}\" title=\"Activate/extend Pro 31 days\">+31d Pro</button>\n<button class=\"btn btn--xs\" data-act=\"free\" data-id=\"${u.id}\" title=\"Downgrade to Free\">Free</button>\n${u.status === \"suspended\"\n  ? `<button class=\"btn btn--xs\" data-act=\"unsuspend\" data-id=\"${u.id}\">Unsuspend</button>`\n  : `<button class=\"btn btn--xs btn--danger\" data-act=\"suspend\" data-id=\"${u.id}\">Suspend</button>`}\n<button class=\"btn btn--xs\" data-act=\"reset-link\" data-id=\"${u.id}\" title=\"Generate a 24h password reset link\">Reset link</button>\n</td></tr>`;\n  }).join(\"\");\n  $(\"usersBody\").querySelectorAll(\"button[data-act]\").forEach((b) => b.addEventListener(\"click\", () => action(b)));\n}\n\nasync function action(btn) {\n  const act = btn.dataset.act, userId = btn.dataset.id;\n  if (act === \"suspend\" && !confirm(\"Suspend this account? Their page goes offline and they can't sign in.\")) return;\n  btn.disabled = true;\n  try {\n    const body = { userId, action: act };\n    if (act === \"pro\") {\n      const amt = prompt(\"Amount they paid you in USD (for the revenue ledger — 0 if comped):\", \"29\");\n      if (amt === null) { btn.disabled = false; return; }\n      body.amountUsd = Number(amt) || 0;\n    }\n    const d = await api(\"/api/admin/action\", { method: \"POST\", headers: { \"content-type\": \"application/json\" }, body: JSON.stringify(body) });\n    if (!d.ok) { alert(d.error || \"Failed\"); btn.disabled = false; return; }\n    if (act === \"reset-link\") {\n      try { await navigator.clipboard.writeText(d.link); } catch {}\n      prompt(`Reset link for ${d.email} (valid 24h, copied to clipboard):`, d.link);\n      btn.disabled = false;\n      return;\n    }\n    await loadUsers();\n    const ov = await api(\"/api/admin/overview\");\n    $(\"s_pro\").textContent = ov.pro; $(\"s_rev\").textContent = \"$\" + Number(ov.revenue || 0).toLocaleString();\n  } catch { btn.disabled = false; }\n}\n\nasync function loadLeads() {\n  const d = await api(\"/api/admin/leads\");\n  const rows = d.leads || [];\n  $(\"leadsEmpty\").hidden = rows.length > 0;\n  $(\"leadsBody\").innerHTML = rows.map((l) =>\n    `<tr><td>${esc(l.handle)}</td><td>${esc(l.casino)}</td><td>${esc(l.contact)}</td><td class=\"note\">${esc(l.note)}</td><td>${when(l.created_at)}</td></tr>`\n  ).join(\"\");\n}\n\nasync function loadPayments() {\n  const d = await api(\"/api/admin/payments\");\n  const rows = d.payments || [];\n  $(\"payEmpty\").hidden = rows.length > 0;\n  $(\"payBody\").innerHTML = rows.map((p) => {\n    const tone = [\"finished\", \"manual\"].includes(p.status) ? \"good\" : [\"failed\", \"expired\", \"refunded\"].includes(p.status) ? \"bad\" : \"muted\";\n    return `<tr><td>${esc(p.email || p.user_id)}</td><td>${esc(p.provider)}</td><td class=\"ta-r\">$${Number(p.amount_usd).toFixed(2)}</td><td>${pill(p.status, tone)}</td><td>${when(p.created_at)}</td></tr>`;\n  }).join(\"\");\n}\n\n$(\"logout\").addEventListener(\"click\", async (e) => { e.preventDefault(); await fetch(\"/api/auth/logout\", { method: \"POST\" }); location.href = \"/login\"; });\ninit();\n";
export const landing_css = "/* RankUp marketing — raw, monochrome, one accent. */\n:root{--bg:#0b0b0c;--panel:#101012;--panel-2:#151518;--line:#212125;--line-2:#2c2c31;--ink:#ededf0;--ink-soft:#a1a1a9;--ink-mute:#67676f;--accent:#c8ff00;--accent-ink:#0b0b0c;--mono:\"JetBrains Mono\",ui-monospace,Menlo,monospace;--sans:\"Inter\",system-ui,-apple-system,Segoe UI,Roboto,sans-serif}\n*{box-sizing:border-box}html,body{margin:0}\nbody{background:var(--bg);color:var(--ink);font-family:var(--sans);font-size:16px;line-height:1.55;-webkit-font-smoothing:antialiased}\na{color:var(--ink)}::selection{background:var(--accent);color:var(--accent-ink)}\n.mono{font-family:var(--mono)}\n.label{font-family:var(--mono);font-size:11px;letter-spacing:.16em;text-transform:uppercase;color:var(--ink-mute)}\n.wrap{max-width:1080px;margin:0 auto;padding:0 24px}\n.btn{display:inline-flex;align-items:center;gap:8px;font-weight:600;font-size:14px;padding:11px 18px;border-radius:8px;border:1px solid var(--line-2);background:var(--panel-2);color:var(--ink);cursor:pointer;text-decoration:none;transition:border-color .15s}\n.btn:hover{border-color:var(--ink-mute)}.btn--accent{background:var(--accent);color:var(--accent-ink);border-color:var(--accent)}.btn--accent:hover{filter:brightness(1.05)}\n.btn--sm{padding:6px 11px;font-size:13px}\nnav.top{display:flex;align-items:center;justify-content:space-between;padding:20px 0;border-bottom:1px solid var(--line)}\nnav.top .brand{font-weight:800;font-size:19px;letter-spacing:-.02em}nav.top .brand b{color:var(--accent)}\nnav.top .links{display:flex;gap:20px;align-items:center;font-size:14px}\nnav.top .links a{color:var(--ink-soft);text-decoration:none}nav.top .links a:hover{color:var(--ink)}\n.hero{display:grid;grid-template-columns:1.15fr .85fr;gap:48px;align-items:center;padding:72px 0 40px}\n.hero h1{font-size:clamp(34px,5vw,56px);line-height:1.02;letter-spacing:-.03em;margin:0 0 20px;max-width:15ch}\n.hero p.lead{font-size:18px;color:var(--ink-soft);max-width:46ch;margin:0 0 28px}\n.hero .cta{display:flex;gap:12px;flex-wrap:wrap}.hero .fine{margin-top:16px;font-size:13px;color:var(--ink-mute)}\n.spec{border:1px solid var(--line);border-radius:12px;background:var(--panel);overflow:hidden}\n.spec .spec-h{padding:11px 14px;border-bottom:1px solid var(--line);font-family:var(--mono);font-size:12px;color:var(--ink-mute);display:flex;justify-content:space-between}\n.spec .spec-row{display:flex;justify-content:space-between;padding:11px 14px;border-bottom:1px solid var(--line);font-size:14px}\n.spec .spec-row:last-child{border-bottom:none}.spec .spec-row span:first-child{color:var(--ink-soft)}.spec .spec-row span:last-child{font-family:var(--mono)}\n.spec .dot{color:var(--accent)}\n@media(max-width:820px){.hero{grid-template-columns:1fr;gap:32px;padding:48px 0 24px}}\nsection{padding:52px 0;border-top:1px solid var(--line)}\nh2.sec{font-size:28px;letter-spacing:-.02em;margin:0 0 8px}p.sec-sub{color:var(--ink-soft);max-width:56ch;margin:0 0 32px}\n.steps{display:flex;flex-direction:column;gap:2px}\n.step{display:grid;grid-template-columns:52px 1fr;gap:20px;padding:22px 0;border-bottom:1px solid var(--line);align-items:baseline}\n.step:last-child{border-bottom:none}.step .n{font-family:var(--mono);font-size:15px;color:var(--accent)}.step h3{margin:0 0 4px;font-size:17px}.step p{margin:0;color:var(--ink-soft);max-width:60ch}\n.example{border:1px solid var(--line);border-radius:12px;overflow:hidden;background:var(--panel)}\n.example .bar{display:flex;justify-content:space-between;padding:10px 14px;border-bottom:1px solid var(--line);font-family:var(--mono);font-size:12px;color:var(--ink-mute)}\n.example iframe{width:100%;height:560px;border:0;display:block;background:#06040c}\ntable.pricing{width:100%;border-collapse:collapse;font-size:15px}\ntable.pricing th,table.pricing td{padding:14px 16px;border-bottom:1px solid var(--line);text-align:left}\ntable.pricing thead th{font-family:var(--mono);font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-mute);font-weight:500}\ntable.pricing td.plan{font-weight:600}table.pricing td.price{font-family:var(--mono)}\ntable.pricing tr td:last-child,table.pricing tr th:last-child{text-align:right}.pro-row td{background:var(--panel)}\n.lead{display:grid;grid-template-columns:1fr 1fr;gap:16px 20px;max-width:640px}.lead .full{grid-column:1 / -1}\n.lead label{display:block;font-size:13px;color:var(--ink-soft);margin-bottom:6px}\n.lead input,.lead textarea{width:100%;background:var(--panel);border:1px solid var(--line-2);border-radius:8px;padding:10px 12px;color:var(--ink);font-family:var(--sans);font-size:14px}\n.lead input:focus,.lead textarea:focus{outline:none;border-color:var(--accent)}.lead .status{grid-column:1/-1;font-size:14px;color:var(--ink-mute);min-height:20px}\n@media(max-width:640px){.lead{grid-template-columns:1fr}}\nfooter{border-top:1px solid var(--line);padding:32px 0;color:var(--ink-mute);font-size:13px;display:flex;justify-content:space-between;flex-wrap:wrap;gap:12px}\n";
