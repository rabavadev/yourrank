{
  "name": "yourrank",
  "private": true,
  "version": "0.2.0",
  "workspaces": ["apps/leaderboard", "apps/bot"],
  "dependencies": {
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "@types/node": "^20.14.10"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "build": "cd apps/leaderboard && bun run build",
    "build:bot": "cd apps/bot && bun run build"
  }
}
