{
  "name": "yourrank",
  "private": true,
  "version": "0.2.0",
  "workspaces": ["apps/leaderboard", "apps/bot", "apps/monitor"],
  "dependencies": {
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "toucan-js": "^4.1.1",
    "wrangler": "4.106.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "build": "cd apps/leaderboard && bun run build",
    "build:bot": "cd apps/bot && bun run build"
  }
}
